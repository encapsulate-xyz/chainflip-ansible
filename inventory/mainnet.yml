---
# maintains mainnet inventory grouped by project names
all:
  vars:
    env: mainnet
  children:
    chainflip:
      hosts:
        validator.chainflip.mainnet.encapsulate.xyz:
          type: validator
